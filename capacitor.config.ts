import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "mx.supizza.app",
  appName: "supizza-pwa",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
